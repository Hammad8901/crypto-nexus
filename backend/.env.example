# Copy this to .env and fill in values

# Optional — only needed for private exchange data
BINANCE_API_KEY=
BINANCE_SECRET=

# Optional — higher rate limits
COINGECKO_API_KEY=

# CORS — comma-separated allowed origins (leave * for open access)
ALLOWED_ORIGINS=*

# App environment
ENV=production
