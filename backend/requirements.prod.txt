# Lightweight production requirements for Hugging Face Spaces
# Full ML stack (TensorFlow, PyTorch) removed — models run in heuristic mode
# To enable full ML: pip install tensorflow torch transformers

fastapi==0.111.0
uvicorn[standard]==0.30.0
websockets==12.0
python-dotenv==1.0.1

# Data processing
numpy==1.26.4
pandas==2.2.2
requests==2.32.3
httpx==0.27.0

# Crypto market data
ccxt==4.3.34

# Sentiment (lightweight, no GPU)
vaderSentiment==3.3.2
nltk==3.8.1

# Lightweight ML
scikit-learn==1.5.0
